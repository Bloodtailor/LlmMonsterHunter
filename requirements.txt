# Aggregator - `pip install -r requirements.txt` installs the full game.
# base.txt: everything but the LLM engine (what CI installs)
# llm.txt:  llama-cpp-python (CUDA wheel nuance documented there)
-r requirements/base.txt
-r requirements/llm.txt
