# The API-first game needs only base.txt (text runs on DeepSeek, art on
# Gemini - keys pasted in-game). The local-LLM escape hatch installs
# llama-cpp-python separately via:
#     python -m setup.setup_environment --local-extras
# (see requirements/llm.txt for the CUDA wheel nuance).
-r requirements/base.txt
