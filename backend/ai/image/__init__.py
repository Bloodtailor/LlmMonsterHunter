# Image Generation Package - the cloud image seam
# Mirrors the LLM provider layout: image_settings resolves WHO paints
# (the game_settings row), gemini.py speaks the API, processor.py runs
# one queued request end to end, paths.py owns the outputs tree.
