// Table Components Export - Clean imports for all table-related components
// Allows for clean imports like: import { Table, TableHead, TableCell } from 'shared/ui/Table'

export { 
  default as Table, 
  TableHead, 
  TableBody, 
  TableRow, 
  TableHeaderCell, 
  TableCell,
  TABLE_SIZES 
} from './Table.js';