// Explosions Components Export - Clean imports for all explosion-related components
// Allows for clean imports like: import { Explosion, EXPLOSION_TYPES, EXPLOSION_SIZES } from 'shared/ui/Explosions'

export { 
  default as Explosion, 
  EXPLOSION_SIZES, 
  EXPLOSION_TYPES,
  EXPLOSION_COLORS 
} from './Explosion.js';