# Everything the backend needs EXCEPT the LLM engine.
# CI installs only this file (llama_cpp imports lazily; offline suites
# never touch it) - see requirements/llm.txt for the model engine.
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-CORS==4.0.0
PyMySQL==1.1.0
cryptography>=41.0.0
python-dotenv==1.0.0
requests==2.31.0
